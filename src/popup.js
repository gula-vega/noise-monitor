/**
 * Meet Mic Volume Alert — Popup Script
 *
 * Currently a static help page. This script is a placeholder for any
 * future interactive popup features (e.g. showing current threshold,
 * toggling the extension, linking to options).
 */

(() => {
  "use strict";

  // Future: could read/display current threshold from chrome.storage
  // chrome.storage.sync.get("threshold", (result) => { ... });
})();
